package edu.ncsu.csc573.project.common.messages;

import java.util.Set;

public class JoinLeaveParameter extends Parameter {
	/*private PublishSearchParameter pubsearchParam;
	private TableParam tableParam;

	public void add(EnumParamsType param, Object value) {
		if (param != EnumParamsType.PEERID || param != EnumParamsType.FIRSTHASH
				|| param != EnumParamsType.LASTHASH
				|| param != EnumParamsType.MYIPADDRESS) {
			super.add(param, value);
		}
		
		else if(pubsearchParam == null) {
				pubsearchParam = new PublishSearchParameter();
			}
			pubsearchParam.add(param, value);
		} else {
			
		}
	}

	public Object getParamValue(EnumParamsType paramType) {
		if()
	}

	public Set<EnumParamsType> getAllParams() {
		// TODO Auto-generated method stub
		return null;
	}*/

}
